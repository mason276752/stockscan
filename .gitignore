node_modules/
web/dist/
*.log
.env
# 資料不進 main：data/store（財報與評分）與 data/bars（已結束年份的日線）在 `data` branch，
# 由 Pages 的 workflow 更新；本機用 `git restore --source=FETCH_HEAD -- data/store data/bars` 取回。
# cache.sqlite、今年的 head.zst 等其他 data/ 底下的東西本來就不進 git。
data/
web/dist-static/
